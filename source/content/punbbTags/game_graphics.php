<?php
if (!isset($config)) exit('Config must be loaded.'); // выйти, если не загружены настройки
General::$data['punbb_tag_groups']['game_graphics'] = array ( // описание группы тэгов
	'name' => 'Графика', // имя группы
	'tags' => array( // тэги
		162 // 3D
	)
);
