<?php
if (!isset($config)) exit('Config must be loaded.'); // выйти, если не загружены настройки
General::$data['punbb_tag_groups']['video_platforms'] = array ( // описание группы тэгов
	'name' => 'Платформы', // имя группы
	'tags' => array ( // тэги
		362 // S60v5
	)
);
